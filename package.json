{
  "name": "civagent",
  "version": "1.0.0",
  "private": true,
  "description": "Connected Organization Intelligence Agent for designing AI-native operating models.",
  "scripts": {
    "start": "python3 server.py",
    "setup:python": "python3 -m pip install -r requirements.txt",
    "static": "python3 -m http.server 8080",
    "check": "node --check script.js && python3 -m py_compile server.py"
  }
}
